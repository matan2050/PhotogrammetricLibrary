#include "GridBase.h"

GridBasedModels::Grid::Grid(float _start_x, float _start_y, float _step_x, float _step_y)
{
    start_x = _start_x;
    start_y = _start_y;
    step_x = _step_x;
    step_y = _step_y;
}